#cloud-config
ssh_authorized_keys:
%{ for key in ssh_authorized_keys ~}
  - ${key}
%{ endfor ~}

package_update: true
packages:
  - openssh-server
  - python3
  - sudo

runcmd:
  - echo "bootstrap complete" > /var/log/bootstrap-ready.log
